# Dependances du bot Kalshi -- correspond aux imports reels du code.
# NB : le paquet s'appelle python-dotenv (PAS "dotenv", qui est un autre
# paquet obsolete sur PyPI).
requests
python-dotenv
anthropic
cryptography
